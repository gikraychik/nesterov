#pragma once
#include <iostream>
#include <Address.h>
#include <Analysis.h>
#include <exception>
#include <AvlTree.h>
#include <cstring>

using namespace std;

int main(int argc, char **argv)
{
	/*try
	{
		Analysis analis("10records");
		for (int i = 0; i < analis.requests().size(); i++)
		{
			cout << analis[i].addr << endl;
		}
	}
	catch (exception e)
	{
		cerr << e.what() << endl;
		cerr << "Program terminated." << endl;
		return 1;
	}*/
	system("pause");
	return 0;
}